name: Build Jellyfin AndroidTV

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download subtitle patch from your fork
        run: |
          curl -L https://raw.githubusercontent.com/Activusto/jellyfin-androidtv-Subtitle-Offset/master/jellyfin-androidtv-subtitle-offset-git.patch -o subtitle-offset.patch

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Apply subtitle offset patch (verbose)
        run: |
          echo "=== Applying patch ==="
          git apply --verbose subtitle-offset.patch 2>&1 || git apply --stat subtitle-offset.patch
          echo "=== Patch apply complete ==="

      - name: Decode keystore
        run: echo "$KEYSTORE_B64" | base64 -d > keystore.jks
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_B64 }}

      - name: Create gradle.properties
        run: |
          echo "keystore.file=../keystore.jks" >> gradle.properties
          echo "keystore.password=${{ secrets.KEYSTORE_PASSWORD }}" >> gradle.properties
          echo "signing.key.alias=${{ secrets.KEY_ALIAS }}" >> gradle.properties
          echo "signing.key.password=${{ secrets.KEYSTORE_PASSWORD }}" >> gradle.properties

      - name: Build signed APK
        run: ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: jellyfin-androidtv-subtitle-signed
          path: app/build/outputs/apk/release/*.apk
